# Frontend environment (public vars must be prefixed NEXT_PUBLIC_).
# Copy to .env.local for local runs. In the production Docker build these are
# passed as build args (they are inlined into the bundle at build time).

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_API_DOCS_URL=http://localhost:8000/docs
