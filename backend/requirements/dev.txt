# Development dependencies (SRS §30, §35).
# Source of truth is ../pyproject.toml ([dependency-groups] dev) + uv.lock.
-r base.txt
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
ruff>=0.7
black>=24.10
mypy>=1.13
types-pyyaml>=6.0
